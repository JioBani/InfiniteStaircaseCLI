#ifndef GRAPHIC_H
#define	GRAPHIC_H

#include "screen_control.h"

extern char graphic_stair[2][9];

extern char graphic_player[4][9];

extern RenderInfo renderinfo_stair;

extern RenderInfo renderinfo_bigStair;

extern RenderInfo renderinfo_bigStairStd;

extern RenderInfo renderinfo_bigStairStdM;

extern RenderInfo renderinfo_building;
 
extern RenderInfo renderinfo_playerLeft;

extern RenderInfo renderinfo_playerRight;

extern RenderInfo renderinfo_playerClimbingRight;

extern RenderInfo renderinfo_playerClimbingLeft;


// ===================================//


extern RenderInfo renderinfo_ground;

extern RenderInfo renderinfo_coin;

extern RenderInfo renderinfo_cloud0;

extern RenderInfo renderinfo_cloud1;

extern RenderInfo renderinfo_cloud2;

extern RenderInfo renderinfo_cloud3;

extern RenderInfo renderinfo_cloud4;

extern RenderInfo renderinfo_cloud5;

extern RenderInfo renderinfo_balloonRed;

extern RenderInfo renderinfo_balloonOrange;

extern RenderInfo renderinfo_balloonGreen;

extern RenderInfo renderinfo_bird;

extern RenderInfo renderinfo_sun;

// ===================================//

extern RenderInfo renderinfo_PREES_R;

extern RenderInfo renderinfo_TO_RESTART;

extern RenderInfo renderinfo_SCORE_Colon;

// ========== number_print ========== //

extern RenderInfo renderinfo_number0;

extern RenderInfo renderinfo_number1;

extern RenderInfo renderinfo_number2;

extern RenderInfo renderinfo_number3;

extern RenderInfo renderinfo_number4;

extern RenderInfo renderinfo_number5;

extern RenderInfo renderinfo_number6;

extern RenderInfo renderinfo_number7;

extern RenderInfo renderinfo_number8;

extern RenderInfo renderinfo_number9;

extern RenderInfo renderinfo_numberNULL;



extern RenderInfo renderinfo_timer;

extern RenderInfo renderinfo_timer0;

extern RenderInfo renderinfo_timerIn;

extern RenderInfo renderinfo_GO;

extern RenderInfo renderinfo_UP_EXEX;

extern RenderInfo renderinfo_PRESS_W;

extern RenderInfo renderinfo_TO_START;

extern RenderInfo renderinfo_W;

extern RenderInfo renderinfo_SPACE;

extern RenderInfo renderinfo_UpArrow;

extern RenderInfo renderinfo_SideArrow;

#endif
